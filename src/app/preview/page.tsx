import ExperienceCanvas from "@/components/ExperienceCanvas";

/** Screenshot-QA route: renders only the WebGL experience, full screen. */
export default function PreviewPage() {
  return <ExperienceCanvas />;
}
