// Placeholder — replaced during final assembly (loader → slider → menu state machine).
export default function Home() {
  return (
    <main className="grid min-h-screen place-items-center bg-bg text-white">
      <p className="font-display tracking-widest text-sm opacity-60">ORANO · loading experience…</p>
    </main>
  );
}
